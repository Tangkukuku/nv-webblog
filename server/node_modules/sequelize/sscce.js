'use strict';

const Sequelize = require('./index');
const sequelize1 = new Sequelize({
  dialect: 'mysql'
});
const sequelize2 = new Sequelize({
  dialect: 'mysql'
});

console.log(sequelize1.literal === sequelize2.literal);